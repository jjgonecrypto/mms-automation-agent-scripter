mms-automation-agent-scripter
=============================

Bash script to grab the latest MMS automation agent, configure and start it

Usage:

* Download, configure and start: `start-agent <groupId> <appKey> <baseUrl>`
* Restart only: `start-agent restart`
